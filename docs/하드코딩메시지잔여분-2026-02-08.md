# 서버로그 하드코딩 메시지 잔여분

**작성일**: 2026-02-08
**조사 범위**: `server/csa` 디렉토리 전체
**대상**: i18n 미적용 하드코딩 메시지 (한국어 + 영어)

---

## 📊 요약

| 항목 | 개수 |
|------|------|
| **한국어 메시지** | 22개 |
| **영어 메시지** | 43개 |
| **총계** | **65개** |

---

## 🔴 한국어 하드코딩 메시지 (22개)

### server/csa/aiwork/ai_providers.py (5개)

| 라인 | 메시지 | 상태 |
|------|--------|------|
| 280 | `logger.warning(f"알 수 없는 AI Provider: {name}, Google로 기본 설정")` | 활성화 |
| 288 | `logger.warning(f"알 수 없는 AI Provider: {self.current_provider_name}, Google로 기본 설정")` | 주석 처리 |
| 325 | `logger.warning(f"현재 Provider '{self.current_provider_name}'가 사용 불가능합니다.")` | 주석 처리 |
| 329 | `logger.info("Google Gemini로 폴백합니다.")` | 주석 처리 |
| 334 | `logger.info(f"AI Provider '{self.current_provider_name}' 사용, 모델: {provider.get_model_name()}")` | 주석 처리 |

---

### server/csa/services/java_analysis/mybatis.py (1개)

| 라인 | 메시지 |
|------|--------|
| 71 | `logger.warning(f"AI SQL 분석 실패 ({mapper.name}.{sql_dict.get('id', '')}): {e}")` |

**필요한 i18n 키**:
```json
"java_analysis.ai_sql_analysis_failed": "AI SQL 분석 실패 ({mapper_name}.{sql_id}): {error}"
```

---

### server/csa/services/java_analysis/part1.py (8개)

| 라인 | 메시지 |
|------|--------|
| 226 | `logger.debug(f"DTO inner 소스 저장 건너뜀: {inner_class_full_name}")` |
| 377 | `logger.debug(f"Inner class 메서드 Cognitive Complexity 계산 실패 ({inner_class_full_name}.{method_name}): {e}")` |
| 398 | `logger.debug(f"Inner class code_complexity 계산 실패 ({inner_class_full_name}): {e}")` |
| 507 | `logger.warning(f"AI Class 분석 실패 ({class_name}): {e}")` |
| 516 | `logger.debug(f"DTO 소스 저장 건너뜀: {class_name}")` |
| 609 | `logger.debug(f"DTO 메서드 분석 생략: {class_name} (sub_type={sub_type})")` |
| 759 | `logger.debug(f"메서드 Cognitive Complexity 계산 실패 ({class_name}.{declaration.name}): {e}")` |
| 869 | `logger.debug(f"Class code_complexity 계산 실패 ({class_name}): {e}")` |

**필요한 i18n 키**:
```json
{
  "java_analysis.dto_inner_source_skipped": "DTO inner 소스 저장 건너뜀: {class_name}",
  "java_analysis.inner_class_method_complexity_failed": "Inner class 메서드 Cognitive Complexity 계산 실패 ({class_name}.{method_name}): {error}",
  "java_analysis.inner_class_complexity_failed": "Inner class code_complexity 계산 실패 ({class_name}): {error}",
  "java_analysis.ai_class_analysis_failed": "AI Class 분석 실패 ({class_name}): {error}",
  "java_analysis.dto_source_skipped": "DTO 소스 저장 건너뜀: {class_name}",
  "java_analysis.dto_method_analysis_skipped": "DTO 메서드 분석 생략: {class_name} (sub_type={sub_type})",
  "java_analysis.method_complexity_failed": "메서드 Cognitive Complexity 계산 실패 ({class_name}.{method_name}): {error}",
  "java_analysis.class_complexity_failed": "Class code_complexity 계산 실패 ({class_name}): {error}"
}
```

---

### server/csa/services/java_analysis/part2.py (3개)

| 라인 | 메시지 |
|------|--------|
| 63 | `logger.debug(f"DTO 메서드 분석 생략: {class_name} (sub_type={sub_type})")` |
| 166 | `logger.debug(f"메서드 Cognitive Complexity 계산 실패 ({class_name}.{declaration.name}): {e}")` |
| 276 | `logger.debug(f"Class code_complexity 계산 실패 ({class_name}): {e}")` |

**필요한 i18n 키**:
- `java_analysis.dto_method_analysis_skipped` (part1.py와 동일)
- `java_analysis.method_complexity_failed` (part1.py와 동일)
- `java_analysis.class_complexity_failed` (part1.py와 동일)

---

### server/csa/services/java_analysis/part4.py (4개)

| 라인 | 메시지 |
|------|--------|
| 374 | `logger.debug(f"Package 추출 실패 (무시): {file_path} - {e}")` |
| 671 | `logger.info(f"Bean 의존성 해결 중... (총 {stats['beans']}개 Bean, 시간이 걸릴 수 있습니다)")` |
| 680 | `logger.info(f"Bean 의존성 해결 완료 ({bean_elapsed:.1f}초)")` |
| 729 | `logger.info(f".csaignore로 {excluded_count}개 파일 제외됨")` |

**필요한 i18n 키**:
```json
{
  "java_analysis.package_extraction_failed": "Package 추출 실패 (무시): {file_path} - {error}",
  "java_analysis.bean_dependency_resolving_detailed": "Bean 의존성 해결 중... (총 {count}개 Bean, 시간이 걸릴 수 있습니다)",
  "java_analysis.bean_dependency_complete_detailed": "Bean 의존성 해결 완료 ({elapsed:.1f}초)",
  "java_analysis.csaignore_files_excluded_count": ".csaignore로 {count}개 파일 제외됨"
}
```

---

### server/csa/services/java_analysis/project.py (1개)

| 라인 | 메시지 |
|------|--------|
| 2007 | `logger.error(f"❌ 예외 발생: {file_name} - {e}")` |

**필요한 i18n 키**:
```json
"java_analysis.exception_occurred": "예외 발생: {file_name} - {error}"
```

---

## 🔵 영어 하드코딩 메시지 (43개)

### server/csa/aiwork/ai_analyzer.py (10개)

| 라인 | 메시지 | 유형 |
|------|--------|------|
| 242 | `logger.warning(f"  - {attempt}")` | 디버그용 |
| 782 | `logger.debug(f"[AsyncRetry] Error: {error_type}, Retryable: {is_retryable}, RetryCount: {retry_count}/{max_retries}")` | 디버그용 |
| 1400 | `logger.error("Class analysis prompt not found")` | 에러 |
| 1410 | `logger.info(f"AI Class Analysis Start: {class_name} ({len(source_code)} chars)")` | 정보 |
| 1415 | `logger.info(f"AI Class Analysis Completed: {class_name} ({elapsed:.2f}s)")` | 정보 |
| 1420 | `logger.error(f"AI Class Analysis Failed: {class_name} - {e}")` | 에러 |
| 1445 | `logger.error("Method analysis prompt not found")` | 에러 |
| 1454 | `logger.debug(f"AI Method Analysis Start: {full_name}")` | 디버그용 |
| 1459 | `logger.debug(f"AI Method Analysis Completed: {full_name} ({elapsed:.2f}s)")` | 디버그용 |
| 1464 | `logger.error(f"AI Method Analysis Failed: {full_name} - {e}")` | 에러 |

**필요한 i18n 키**:
```json
{
  "ai_analyzer.class_analysis_prompt_not_found": "Class analysis prompt not found",
  "ai_analyzer.class_analysis_start_sync": "AI Class Analysis Start: {class_name} ({size} chars)",
  "ai_analyzer.class_analysis_complete_sync": "AI Class Analysis Completed: {class_name} ({elapsed:.2f}s)",
  "ai_analyzer.class_analysis_failed_sync": "AI Class Analysis Failed: {class_name} - {error}",
  "ai_analyzer.method_analysis_prompt_not_found": "Method analysis prompt not found",
  "ai_analyzer.method_analysis_start_sync": "AI Method Analysis Start: {identifier}",
  "ai_analyzer.method_analysis_complete_sync": "AI Method Analysis Completed: {identifier} ({elapsed:.2f}s)",
  "ai_analyzer.method_analysis_failed_sync": "AI Method Analysis Failed: {identifier} - {error}"
}
```

---

### server/csa/services/ai_enrichment_service.py (9개)

| 라인 | 메시지 | 유형 |
|------|--------|------|
| 70 | `self.logger.warning(f"Unknown node_type '{node_type}' for clean operation. Skipping cleanup.")` | 경고 |
| 81 | `self.logger.error(f"[CLEAN] Failed to clear ai_description for {label}: {exc}")` | 에러 |
| 162 | `self.logger.warning(f"Project enrichment {project_name} stopped (Class cancelled)")` | 경고 |
| 183 | `self.logger.warning(f"Project enrichment {project_name} stopped (Method cancelled)")` | 경고 |
| 204 | `self.logger.warning(f"Project enrichment {project_name} stopped (SQL cancelled)")` | 경고 |
| 296 | `self.logger.info("No Class nodes found that need AI enrichment")` | 정보 |
| 299 | `self.logger.info(f"Found {total} Class nodes to enrich")` | 정보 |
| 300 | `self.logger.info(f"Processing {total} Class nodes...")` | 정보 |
| 316 | `self.logger.info(f"[{i}/{total}] Class enriched: {class_name}")` | 정보 |

**필요한 i18n 키**:
```json
{
  "ai_enrichment.unknown_node_type": "Unknown node_type '{node_type}' for clean operation. Skipping cleanup.",
  "ai_enrichment.clear_failed": "[CLEAN] Failed to clear ai_description for {label}: {error}",
  "ai_enrichment.class_enrichment_cancelled": "Project enrichment {project_name} stopped (Class cancelled)",
  "ai_enrichment.method_enrichment_cancelled": "Project enrichment {project_name} stopped (Method cancelled)",
  "ai_enrichment.sql_enrichment_cancelled": "Project enrichment {project_name} stopped (SQL cancelled)",
  "ai_enrichment.no_classes_found": "No Class nodes found that need AI enrichment",
  "ai_enrichment.classes_found": "Found {total} Class nodes to enrich",
  "ai_enrichment.processing_classes": "Processing {total} Class nodes...",
  "ai_enrichment.class_enriched": "[{current}/{total}] Class enriched: {class_name}"
}
```

---

### server/csa/services/java_analysis/config.py (2개)

| 라인 | 메시지 |
|------|--------|
| 75 | `logger.error(f"Error parsing YAML file {file_path}: {e}")` |
| 164 | `logger.error(f"Error parsing Properties file {file_path}: {e}")` |

**필요한 i18n 키**:
```json
{
  "java_analysis.yaml_parse_error": "Error parsing YAML file {file_path}: {error}",
  "java_analysis.properties_parse_error": "Error parsing Properties file {file_path}: {error}"
}
```

---

### server/csa/services/java_analysis/mybatis.py (3개)

| 라인 | 메시지 |
|------|--------|
| 108 | `logger.debug(f"Failed to generate SQL Flow for {sql_statement.id}: {e}")` |
| 662 | `logger.error(f"Error parsing XML file {file_path}: {e}")` |
| 665 | `logger.error(f"Error reading XML file {file_path}: {e}")` |

**필요한 i18n 키**:
```json
{
  "java_analysis.sql_flow_generation_failed": "Failed to generate SQL Flow for {sql_id}: {error}",
  "java_analysis.xml_parse_error": "Error parsing XML file {file_path}: {error}",
  "java_analysis.xml_read_error": "Error reading XML file {file_path}: {error}"
}
```

---

### server/csa/services/java_analysis/part1.py (12개)

| 라인 | 메시지 |
|------|--------|
| 423 | `logger.debug(f"Successfully parsed file: {file_path}")` |
| 426 | `logger.debug(f"Parsed package name: {package_name}")` |
| 447 | `logger.error(f"No class declaration found in file: {file_path}")` |
| 492 | `logger.info(f"Skipping AI analysis for {class_name} (source unchanged)")` |
| 710 | `logger.warning(f"AI Analysis skipped for {class_name}.{declaration.name}: AI Analyzer not available")` |
| 712 | `logger.warning(f"AI Analysis skipped for {class_name}.{declaration.name}: Empty method source")` |
| 714 | `logger.info(f"Starting AI Analysis for method: {class_name}.{declaration.name}")` |
| 724 | `logger.info(f"AI Analysis completed for method: {class_name}.{declaration.name}")` |
| 726 | `logger.warning(f"AI Analysis skipped for {class_name}.{declaration.name}: AI Analyzer is_available returned False")` |
| 728 | `logger.error(f"AI Analysis failed for {class_name}.{declaration.name}: {ai_err}")` |
| 872 | `logger.debug(f"Successfully parsed single file: {file_path} (found {len(inner_classes)} inner classes)")` |
| 876 | `logger.error(f"Error parsing file: {e}")` |

**필요한 i18n 키**:
```json
{
  "java_analysis.file_successfully_parsed": "Successfully parsed file: {file_path}",
  "java_analysis.package_name_parsed": "Parsed package name: {package_name}",
  "java_analysis.no_class_found_in_file": "No class declaration found in file: {file_path}",
  "java_analysis.ai_analysis_skipped_unchanged": "Skipping AI analysis for {class_name} (source unchanged)",
  "java_analysis.ai_skipped_unavailable_detail": "AI Analysis skipped for {class_name}.{method_name}: AI Analyzer not available",
  "java_analysis.ai_skipped_empty_source_detail": "AI Analysis skipped for {class_name}.{method_name}: Empty method source",
  "java_analysis.starting_ai_method_detail": "Starting AI Analysis for method: {class_name}.{method_name}",
  "java_analysis.ai_completed_method_detail": "AI Analysis completed for method: {class_name}.{method_name}",
  "java_analysis.ai_skipped_unavailable_check": "AI Analysis skipped for {class_name}.{method_name}: AI Analyzer is_available returned False",
  "java_analysis.ai_failed_method_detail": "AI Analysis failed for {class_name}.{method_name}: {error}",
  "java_analysis.single_file_parsed_success": "Successfully parsed single file: {file_path} (found {inner_classes} inner classes)",
  "java_analysis.file_parsing_error": "Error parsing file: {error}"
}
```

---

### server/csa/services/java_analysis/part2.py (2개)

| 라인 | 메시지 |
|------|--------|
| 279 | `logger.debug(f"Successfully parsed single file: {file_path} (found {len(inner_classes)} inner classes)")` |
| 283 | `logger.error(f"Error parsing file: {e}")` |

**필요한 i18n 키** (part1.py와 동일):
- `java_analysis.single_file_parsed_success`
- `java_analysis.file_parsing_error`

---

### server/csa/services/java_analysis/part4.py (5개)

| 라인 | 메시지 |
|------|--------|
| 591 | `logger.warning("Analysis canceled, cancelling pending tasks...")` |
| 608 | `logger.info(f"총 {total_xml_mappers}개 XML mapper 발견")` |
| 651 | `logger.info(f"총 {total_config_files}개 Config 파일 발견")` |
| 661 | `logger.info(f"Config 파일 처리중 [{i}/{total_config_files}] ({config_percent}%)")` |
| 664 | `logger.info(f"Config 파일 처리 완료 ({total_config_files}개, {config_elapsed:.1f}초)")` |

**상태**: 라인 608, 651, 661, 664는 한국어/영어 혼합 메시지

**필요한 i18n 키**:
```json
{
  "java_analysis.analysis_cancelled_pending_tasks": "Analysis canceled, cancelling pending tasks...",
  "java_analysis.xml_mappers_found": "Found {count} XML mappers",
  "java_analysis.config_files_found": "Found {count} Config files",
  "java_analysis.config_file_processing": "Processing Config files [{current}/{total}] ({percent}%)",
  "java_analysis.config_files_complete": "Config file processing completed ({total} files, {elapsed:.1f}s)"
}
```

---

## 📋 i18n 변환 우선순위

### 우선순위 1 (즉시 변환 권장)
- **한국어 메시지 22개**: 이미 한국어로 작성되어 있으므로 번역이 간단함
- **ai_providers.py**: 5개 중 일부 주석 처리되어 있음, 활성화 부분만 처리

### 우선순위 2 (다음 단계)
- **java_analysis 관련 영어 메시지**: 23개
  - part1.py, part2.py, part4.py, mybatis.py, config.py 등
  - 기존 java_analysis 키와 일관성 유지 필요

### 우선순위 3 (선택적)
- **ai_analyzer.py**: 10개 (legacy synchronous methods)
  - 비동기 메서드가 있으므로 레거시 코드로 판단
  - 필요시 비동기 메서드 로깅과 통합 고려

- **ai_enrichment_service.py**: 9개
  - AI enrichment 전용 키 신규 작성 필요

---

## 🔧 다음 조치

1. **한국어 메시지 변환** (22개)
   - 각 파일별로 누락된 i18n 키 추가
   - ko.json, en.json에 일괄 추가

2. **영어 메시지 분류**
   - 디버그 로그: 콘솔에서만 표시되므로 i18n 불필요 (선택적)
   - 사용자 대면 로그: i18n 필수

3. **테스트**
   - 각 메시지 변환 후 실제 실행하여 로그 확인
   - 한국어/영어 모두 정상 출력 확인

---

**최종 통계**
- 총 처리 대상: **65개**
- 한국어 메시지: 22개
- 영어 메시지: 43개
