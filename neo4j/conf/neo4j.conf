# Neo4j Configuration File
# Neo4j 5.x Community Edition
# 최소 설정만 포함 (대부분은 환경변수로 관리)

# Network Settings
server.default_listen_address=0.0.0.0
#server.bolt.enabled=true
#server.bolt.listen_address=0.0.0.0:7687
dbms.connector.bolt.enabled=true
dbms.connector.bolt.listen_address=0.0.0.0:7687
#server.bolt.tls_level=REQUIRED

server.http.enabled=true
server.http.listen_address=0.0.0.0:7474
server.https.listen_address=0.0.0.0:7474

# Database Settings (환경변수로 오버라이드됨)
initial.dbms.default_database=refinerdb

# Memory Settings (환경변수로 오버라이드됨)
server.memory.heap.initial_size=2G
server.memory.heap.max_size=4G

# Security Settings (환경변수로 오버라이드됨)

server.memory.pagecache.size=512M
server.directories.plugins=/plugins
server.directories.logs=/logs
server.directories.import=/import
dbms.security.procedures.unrestricted=apoc.*
dbms.memory.heap.max_size=4G
dbms.memory.heap.initial_size=2G
dbms.default_database=refinerdb
dbms.memory.transaction.total.max=1.5G
